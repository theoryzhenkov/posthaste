// Account identity/configuration types are the generated wire twins; the
// appearance union is re-exported under the historical name so presentation
// components share one type identity with the `accounts` /
// `accountSettings` families.
export type { AccountAppearance } from '@/gen'
