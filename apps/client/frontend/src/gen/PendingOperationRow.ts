// This file was generated by [ts-rs](https://github.com/Aleph-Alpha/ts-rs). Do not edit this file manually.
import type { AccountId } from "./AccountId";
import type { OperationEntityKind } from "./OperationEntityKind";
import type { OperationId } from "./OperationId";
import type { OperationKind } from "./OperationKind";
import type { OperationState } from "./OperationState";

/**
 * One outbox operation as the client renders it: what it is, what it
 * targets, where it stands. Payload bodies stay in the backend.
 */
export type PendingOperationRow = { id: OperationId, accountId: AccountId, kind: OperationKind, state: OperationState, entityKind: OperationEntityKind, 
/**
 * The targeted message or draft id (possibly still a client-minted temp
 * id awaiting provider reconciliation).
 */
entityId: string, attempts: number, lastError: string | null, 
/**
 * Earliest submission time for a scheduled send (RFC 3339); absent for
 * everything else.
 */
sendAt?: string, createdAt: string, updatedAt: string, };
