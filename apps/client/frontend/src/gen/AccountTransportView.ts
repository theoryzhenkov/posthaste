// This file was generated by [ts-rs](https://github.com/Aleph-Alpha/ts-rs). Do not edit this file manually.
import type { ImapTransportSettings } from "./ImapTransportSettings";
import type { ProviderAuthKind } from "./ProviderAuthKind";
import type { ProviderHint } from "./ProviderHint";
import type { SecretStatus } from "./SecretStatus";
import type { SmtpTransportSettings } from "./SmtpTransportSettings";

/**
 * An account's transport configuration as the client may see it. This view
 * is secrets-safe by construction: the stored credential surfaces only as a
 * redacted [`domain::SecretStatus`] (where it is stored, whether it is
 * configured, an optional label) — never the material, never the lookup key.
 */
export type AccountTransportView = { provider: ProviderHint, auth: ProviderAuthKind, baseUrl: string | null, username: string | null, imap: ImapTransportSettings | null, smtp: SmtpTransportSettings | null, 
/**
 * Redacted credential state; the material itself never travels on the
 * read side and is written only through the dedicated
 * `setAccountSecret` command.
 */
secret: SecretStatus, };
