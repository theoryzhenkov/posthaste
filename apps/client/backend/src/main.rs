//! Binary entry point — placeholder until the implementation shape is decided
//! (see `apps/client/README.md`).

fn main() {
    println!("posthaste-client-backend: scaffolding only — implementation pending");
}
