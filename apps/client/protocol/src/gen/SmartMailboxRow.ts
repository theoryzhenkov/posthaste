// This file was generated by [ts-rs](https://github.com/Aleph-Alpha/ts-rs). Do not edit this file manually.
import type { MailQueryRule } from "./MailQueryRule";
import type { SmartMailboxId } from "./SmartMailboxId";
import type { SmartMailboxKind } from "./SmartMailboxKind";

/**
 * One smart mailbox: its saved configuration (rule included, so the editor
 * needs no second read) plus live unread/total counts.
 */
export type SmartMailboxRow = { id: SmartMailboxId, name: string, kind: SmartMailboxKind, 
/**
 * Identifies built-in smart mailboxes (e.g. `"inbox"`, `"trash"`).
 */
defaultKey: string | null, 
/**
 * The mailbox role whose semantics apply to this view (e.g. `"trash"`),
 * driving contextual actions like Delete Permanently.
 */
role: string | null, parentId?: SmartMailboxId | null, 
/**
 * The saved query rule this mailbox evaluates.
 */
rule: MailQueryRule, unreadMessages: number, totalMessages: number, createdAt: string, updatedAt: string, };
