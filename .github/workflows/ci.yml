name: CI

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  rust:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy
      - uses: Swatinem/rust-cache@v2
        with:
          shared-key: ci-rust
          cache-on-failure: true
      - name: Install Tauri build dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libwebkit2gtk-4.1-dev \
            libgtk-3-dev \
            libayatana-appindicator3-dev \
            librsvg2-dev \
            mold \
            patchelf
      - run: cargo fmt --all -- --check
      # posthaste-bench (profiling harness) is built/run by profile.yml, not here.
      - run: cargo clippy --workspace --all-targets --exclude posthaste-bench --exclude posthaste-client-node-wasm -- -D warnings
      - run: cargo test --workspace --exclude posthaste-bench --exclude posthaste-client-node-wasm

  # The four-crate frontier set is extended by later refactor steps (RFC-L2-architecture-cleanup D15/M8).
  wasm-frontier:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: wasm32-unknown-unknown
      - uses: Swatinem/rust-cache@v2
        with:
          shared-key: ci-wasm-frontier
          cache-on-failure: true
      - run: cargo check --target wasm32-unknown-unknown -p posthaste-replica-core -p posthaste-replica-projector -p posthaste-contract-core -p posthaste-domain-model -p posthaste-link-near-end -p posthaste-client-node-wasm

  frontend:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: oven-sh/setup-bun@v2
      - name: Cache Bun download cache
        uses: actions/cache@v4
        with:
          path: ~/.bun/install/cache
          key: ${{ runner.os }}-bun-${{ hashFiles('bun.lock') }}
          restore-keys: |
            ${{ runner.os }}-bun-
      - run: bun install --frozen-lockfile
      - run: bun --cwd=apps/web run check
      - run: bun --cwd=apps/web run test
      - run: bun --cwd=apps/web run build

  site:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: oven-sh/setup-bun@v2
      - name: Cache Bun download cache
        uses: actions/cache@v4
        with:
          path: ~/.bun/install/cache
          key: ${{ runner.os }}-bun-${{ hashFiles('bun.lock') }}
          restore-keys: |
            ${{ runner.os }}-bun-
      - run: bun install --frozen-lockfile
      - run: bun --cwd=apps/site run check
      - run: bun --cwd=apps/site run test
      - run: bun --cwd=apps/site run build

  mcp:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: oven-sh/setup-bun@v2
      - name: Cache Bun download cache
        uses: actions/cache@v4
        with:
          path: ~/.bun/install/cache
          key: ${{ runner.os }}-bun-${{ hashFiles('bun.lock') }}
          restore-keys: |
            ${{ runner.os }}-bun-
      - run: bun install --frozen-lockfile
      - name: Check generated API schema is up to date
        run: |
          bun --cwd=apps/mcp run api:generate
          git diff --exit-code apps/mcp/src/schema.gen.ts
      - run: bun --cwd=apps/mcp run typecheck
      - run: bun --cwd=apps/mcp run build

  lab:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - uses: oven-sh/setup-bun@v2
      - uses: extractions/setup-just@v3
      - uses: Swatinem/rust-cache@v2
        with:
          shared-key: ci-lab-rust
          cache-on-failure: true
      - name: Cache Bun download cache
        uses: actions/cache@v4
        with:
          path: ~/.bun/install/cache
          key: ${{ runner.os }}-bun-${{ hashFiles('bun.lock') }}
          restore-keys: |
            ${{ runner.os }}-bun-
      - name: Install Linux linker
        run: |
          sudo apt-get update
          sudo apt-get install -y mold
      - run: bun install --frozen-lockfile
      - run: just lab tauri-check
      - name: Lab smoke
        id: lab_smoke
        run: just lab smoke
      - name: Upload lab smoke artifacts
        if: ${{ failure() && steps.lab_smoke.outcome == 'failure' }}
        uses: actions/upload-artifact@v4
        with:
          name: lab-smoke-${{ github.run_id }}-${{ github.run_attempt }}
          path: |
            target/lab/smoke/*/manifest.json
            target/lab/smoke/*/summary.json
            target/lab/smoke/*/artifacts/**
          if-no-files-found: warn
          retention-days: 14

  client-node-wasm:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: wasm32-unknown-unknown
      - uses: oven-sh/setup-bun@v2
      - uses: extractions/setup-just@v3
      - uses: Swatinem/rust-cache@v2
        with:
          shared-key: ci-client-node-wasm
          cache-on-failure: true
      - uses: jetli/wasm-bindgen-action@v0.2.0
        with:
          version: "0.2.117"
      - name: Install binaryen (wasm-opt)
        run: |
          sudo apt-get update
          sudo apt-get install -y binaryen
      - name: Build the client node WASM bundle
        env:
          SKIP_WASM_OPT: 1
        run: just build-client-node-wasm
      - name: Verify committed JS binding is fresh
        # The .wasm binary is not byte-diffed (binaryen output can vary across
        # toolchain builds). The wasm-bindgen JS glue is deterministic given
        # the pinned version, so a stale boundary surfaces here. The .d.ts
        # companion files are also generated by the same command but their export
        # declaration order can shift between wasm-bindgen builds without
        # changing the API; they are validated by the TypeScript build instead.
        run: |
          git diff --exit-code \
            apps/web/src/runtime/wasm/posthaste_client_node_wasm.js
      - run: bun install --frozen-lockfile
      - name: Smoke-test the WASM boundary
        run: bun --cwd=apps/web test test/replicaWasmSmoke.test.ts

  docs:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v5
        with:
          enable-cache: true
      - name: Verify endpoint inventory is up to date
        run: |
          python3 tools/docs/gen_endpoints.py
          git diff --exit-code docs/api/endpoints.md
      - run: uv run mkdocs build --strict
