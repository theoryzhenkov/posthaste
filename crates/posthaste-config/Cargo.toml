[package]
name = "posthaste-config"
version = "0.1.0"
edition = "2021"

[dependencies]
posthaste-domain-model = { path = "../posthaste-domain-model" }
posthaste-domain-service = { path = "../posthaste-domain-service" }
dirs = { workspace = true }
serde = { workspace = true }
toml = { workspace = true }
# Format/comment-preserving TOML edits so a UI save doesn't clobber hand/LLM
# edits (comments, ordering, unknown sections). Matches `toml = 0.8`'s backend.
toml_edit = "0.22"

[dev-dependencies]
tempfile = { workspace = true }

[lints]
workspace = true
