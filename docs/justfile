# Docs (MkDocs) recipes

set working-directory := '..'

# Regenerate the API endpoint inventory from openapi.json (committed artifact)
gen-endpoints:
    python3 tools/docs/gen_endpoints.py

# Serve docs locally
serve: gen-endpoints
    uv run mkdocs serve

# Build docs
build: gen-endpoints
    uv run mkdocs build --strict
